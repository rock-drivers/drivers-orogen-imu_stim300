# Generated from orogen/lib/orogen/templates/tasks/CMakeLists.txt

include(imu_stim300TaskLib)
ADD_LIBRARY(${IMU_STIM300_TASKLIB_NAME} SHARED 
    ${IMU_STIM300_TASKLIB_SOURCES})

add_dependencies(${IMU_STIM300_TASKLIB_NAME}
    regen-typekit)


TARGET_LINK_LIBRARIES(${IMU_STIM300_TASKLIB_NAME}
    ${OrocosRTT_LIBRARIES}
    ${IMU_STIM300_TASKLIB_DEPENDENT_LIBRARIES})
SET_TARGET_PROPERTIES(${IMU_STIM300_TASKLIB_NAME}
    PROPERTIES LINK_INTERFACE_LIBRARIES "${IMU_STIM300_TASKLIB_INTERFACE_LIBRARIES}")

INSTALL(TARGETS ${IMU_STIM300_TASKLIB_NAME}
    RUNTIME DESTINATION bin
    LIBRARY DESTINATION lib/orocos)

INSTALL(FILES ${IMU_STIM300_TASKLIB_HEADERS}
    DESTINATION include/orocos/imu_stim300)

