# Generated from orogen/lib/orogen/templates/tasks/CMakeLists.txt

include(stim300TaskLib)
ADD_LIBRARY(${STIM300_TASKLIB_NAME} SHARED 
    ${STIM300_TASKLIB_SOURCES})

add_dependencies(${STIM300_TASKLIB_NAME}
    regen-typekit)


TARGET_LINK_LIBRARIES(${STIM300_TASKLIB_NAME}
    ${OrocosRTT_LIBRARIES}
    ${STIM300_TASKLIB_DEPENDENT_LIBRARIES})
SET_TARGET_PROPERTIES(${STIM300_TASKLIB_NAME}
    PROPERTIES LINK_INTERFACE_LIBRARIES "${STIM300_TASKLIB_INTERFACE_LIBRARIES}")

INSTALL(TARGETS ${STIM300_TASKLIB_NAME}
    RUNTIME DESTINATION bin
    LIBRARY DESTINATION lib/orocos)

INSTALL(FILES ${STIM300_TASKLIB_HEADERS}
    DESTINATION include/orocos/stim300)

